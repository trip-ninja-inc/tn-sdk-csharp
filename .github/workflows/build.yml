name: PR Decoration - C# SonarQube

on:
    push:
        branches:
            - dev
            - master
            - "**"
    pull_request:
        types: [opened, synchronize, reopened, labeled]

jobs:
  build-test-sonar:
    name: Build, Test & SonarQube
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build project
        run: dotnet build --no-restore --configuration Release

      - name: Run NUnit Tests
        run: |
            dotnet clean
            dotnet build --configuration Release
            dotnet test src/TN.SDK.Test/TN.SDK.Test.csproj \
            --no-build \
            --configuration Release \
            --results-directory TestResults \
            --logger "trx;LogFileName=TestResult.trx"

      - name: Upload NUnit Results
        uses: actions/upload-artifact@v4
        with:
          name: nunit-test-results
          path: TestResults/TestResult.trx

      - name: SonarCloud Analysis
        if: contains(github.event.pull_request.labels.*.name, 'ready-for-sonar-checks')
        run: |
          dotnet tool install --global dotnet-sonarscanner --version 5.*
          dotnet tool install --global dotnet-coverage
          export PATH="$PATH:$HOME/.dotnet/tools"
          dotnet sonarscanner begin \
            /k:"trip-ninja-inc_tn-sdk-csharp" \
            /o:"trip-ninja-inc-1" \
            /d:sonar.host.url="https://sonarcloud.io" \
            /d:sonar.login="${{ secrets.SONAR_TOKEN }}" \
            /d:sonar.cs.vstest.reportsPaths="TestResults/TestResult.trx" \
            /d:sonar.cs.opencover.reportsPaths="**/coverage.cobertura.xml"
          dotnet build --no-restore --configuration Release
          dotnet coverage collect --output coverage.cobertura.xml --format cobertura \
            -- dotnet test src/TN.SDK.Test/TN.SDK.Test.csproj --no-build --configuration Release
          dotnet sonarscanner end /d:sonar.login="${{ secrets.SONAR_TOKEN }}"
